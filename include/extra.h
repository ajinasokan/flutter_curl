#include "curl.h"

// CURLcode curl_easy_setopt_string(CURL *curl, CURLoption option, char *content);

// CURLcode curl_easy_setopt_int(CURL *curl, CURLoption option, int content);

// CURLcode curl_easy_setopt_ptr(CURL *curl, CURLoption option, void *content);

// CURLcode curl_easy_getinfo_long(CURL *handle, CURLINFO info, long *codep);